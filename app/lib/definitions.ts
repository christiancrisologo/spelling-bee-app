export type User = {
  id: string;
  name: string;
  score: number;
  level: string;
};

export type Word = {
  id: string;
  word: string;
  dictionary: string;
  level: string;
  image_url: string;
};
